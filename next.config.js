/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/financial-insights",
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;
